services:
  jenkins:
    image: jenkins/jenkins:lts-jdk17
    container_name: devops_lab-jenkins-1
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    user: "0:0"
    restart: unless-stopped

