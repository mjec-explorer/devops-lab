global:
  resolve_timeout: 5m

route:
  receiver: n8n-webhook
  group_by: ["alertname", "job"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 5m

receivers:
  - name: n8n-webhook
    webhook_configs:
      - url: 'http://n8n:5678/webhook/alertmanager'

